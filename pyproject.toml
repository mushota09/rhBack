[project]
name = "rhback"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "adrf>=0.1.12",
    "adrf-flex-fields==0.1.0",
    "django==5.2",
]
